# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "virtio-loopback-layer"
BBFILE_PATTERN_virtio-loopback-layer = "^${LAYERDIR}/"
BBFILE_PRIORITY_virtio-loopback-layer = "61"

BBFILES_DYNAMIC += " \
    rcar-gen3:${LAYERDIR}/dynamic-layers/rcar-gen3/*/*/*.bbappend \
    raspberrypi:${LAYERDIR}/dynamic-layers/raspberrypi/*/*/*.bbappend \
"

# This should only be incremented on significant changes that will
# cause compatibility issues with other layers
LAYERVERSION_virtio-loopback-layer = "1"
LAYERSERIES_COMPAT_virtio-loopback-layer = "wrynose"

LAYERDEPENDS_virtio-loopback-layer = "core filesystems-layer"
